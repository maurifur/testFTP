# Configuración del SFTP
sftp.host=sftp.example.com
sftp.port=22
sftp.username=usuario
# Use sftp.password o sftp.privateKeyPath + sftp.passphrase
sftp.password=secreto
# sftp.privateKeyPath=/ruta/a/id_rsa
# sftp.passphrase=frase
# sftp.knownHosts=/ruta/a/known_hosts
sftp.remoteDirectory=/ruta/remota

# Configuración de S3
s3.bucket=mi-bucket
s3.prefix=ftp-backup
# s3.region=us-east-1

# Transferencia
transfer.deleteAfterUpload=false
